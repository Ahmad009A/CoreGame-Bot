# nixpacks.toml — Railway build configuration
# yt-dlp for YouTube audio extraction
# ffmpeg for audio transcoding

[phases.setup]
nixPkgs = ["nodejs_22", "python312", "ffmpeg"]

[phases.install]
cmds = [
  "pip install yt-dlp",
  "npm install --legacy-peer-deps"
]

[start]
cmd = "node index.js"
