# nixpacks.toml — Railway build configuration

[variables]
YOUTUBE_DL_SKIP_PYTHON_CHECK = "1"

[phases.setup]
nixPkgs = ["nodejs_22", "ffmpeg", "python3", "yt-dlp"]

[phases.install]
cmds = ["npm install"]

[start]
cmd = "node index.js"
